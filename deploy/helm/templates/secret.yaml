{{- if (include "jira-mcp.useStaticSecret" .) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "jira-mcp.fullname" . }}-secret
  labels:
    {{- include "jira-mcp.labels" . | nindent 4 }}
  {{- with .Values.secret.static.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
  oidc_client_secret: {{ .Values.secret.static.oidcClientSecret | quote }}
  web_token: {{ .Values.secret.static.webToken | quote }}
  cookie_token: {{ .Values.secret.static.cookieToken | quote }}
{{- end }}
